
name: "Codeception Tests"

on:
    schedule:
        - cron: '0 3 * * 1,3,5'
    pull_request:
        branches:
            - "[0-9]+.[0-9]+"
            - "[0-9]+.x"
    push:
        branches:
            - "[0-9]+.[0-9]+"
            - "[0-9]+.x"

env:
    OPENDXP_PROJECT_ROOT: ${{ github.workspace }}
    APP_ENV: test
    OPENDXP_TEST: 1
    OPENDXP_TEST_DB_DSN: "mysql://root@127.0.0.1:33006/opendxp_test"

jobs:
    codeception-tests:
        name: "Codeception tests"
        runs-on: "ubuntu-20.04"
        continue-on-error: ${{ matrix.experimental }}
        strategy:
            matrix:
                include:
                    - { php-version: 8.1, database: "mariadb:10.11", opendxp_version: "", dependencies: highest, experimental: false, require_admin_bundle: true }
                    - { php-version: 8.2, database: "mariadb:10.11", opendxp_version: "", dependencies: highest, experimental: false, require_admin_bundle: true }
                    - { php-version: 8.3, database: "mariadb:10.11", opendxp_version: "1.x-dev", dependencies: highest, experimental: true, require_admin_bundle: true }
        services:
            mariadb:
                image: "${{ matrix.database }}"
                ports:
                    - 33006:3306
                env:
                    MYSQL_ALLOW_EMPTY_PASSWORD: yes

        steps:
            - name: "Checkout code"
              uses: "actions/checkout@v2"

            - name: "Install PHP"
              uses: "shivammathur/setup-php@v2"
              with:
                  coverage: "none"
                  extensions: imagick
                  ini-values: display_errors=On, display_startup_errors=On, error_reporting=32767
                  php-version: "${{ matrix.php-version }}"

            - name: Verify MariaDB connection
              run: |
                  cp .github/ci/files/.my.cnf ~/.my.cnf
                  while ! mysqladmin ping --silent; do
                    sleep 1
                  done

            - name: "Setup OpenDxp environment"
              env:
                  REQUIRE_ADMIN_BUNDLE: "${{ matrix.require_admin_bundle }}"
              run: |
                  mysql -e "CREATE DATABASE opendxp_test CHARSET=utf8mb4;"
                  chmod 755 .github/ci/scripts/setup-opendxp-environment.sh
                  .github/ci/scripts/setup-opendxp-environment.sh

            - name: "Update OpenDxp version"
              env:
                  OPENDXP_VERSION: "${{ matrix.opendxp_version }}"
              run: |
                  if [ ! -z "$OPENDXP_VERSION" ]; then
                        composer require --no-update open-dxp/opendxp:"${OPENDXP_VERSION}"
                  fi    

            - name: "Install dependencies with Composer"
              uses: "ramsey/composer-install@v2"
              with:
                  dependency-versions: "${{ matrix.dependencies }}"

            - name: "Run Codeception"
              run: "vendor/bin/codecept run -c . -vvv --xml"
